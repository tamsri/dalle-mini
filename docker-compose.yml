version: '3.8'
services:
  generator:
    deploy:
      mode: replicated
      replicas: 0
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['4']
            capabilities: [gpu]
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./data:/app/data:rw
    build: generator
    environment:
      - TZ=Europe/Moscow
      - N_PREDICTIONS=1

  upscaler:
    deploy:
      mode: replicated
      replicas: 0
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['5']
            capabilities: [gpu]
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./data:/app/data:rw
    build: upscaler
    environment:
      - TZ=Europe/Moscow
      - RATIO=4

  server:
    deploy:
      mode: replicated
      replicas: 1
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./data:/app/data:rw
    build: server
    environment:
      - TZ=Europe/Moscow
